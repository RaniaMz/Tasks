I wrote the instructions I want to run while building the image in Dockerfie 

then build docker image and name it with 'sudoku' or anything else, by this command ----> [ docker build -t sudoku . ]
then run the docker image ----> [ docker run -it sudoku ]
